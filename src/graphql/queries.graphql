query me {
  me {
    id
    username
    avatarUrl
  }
}

query seeCoffeeShops($lastId: Int) {
  seeCoffeeShops(lastId: $lastId) {
    id
    name
    photos {
      id
      url
    }
    user {
      username
      avatarUrl
    }
    categories {
      id
      name
    }
  }
}
